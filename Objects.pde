//cretae the class of the objects
class Objects {
    float x, y;
    float side;
    color c;
    //create variables
    objetos(float x_, float y_, float side_, ){
        x=x_;
        y=y_;
        lado=lado_;
        c=c_;
    }
    //show the objects
    void show(){
        full(c);
        noStroke();
        rect(x,y,)
    }
}
